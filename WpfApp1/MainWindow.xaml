<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="900" Width="525" Loaded="Window_Loaded"
        Background="#222222">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="MyResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <!--<Window.DataContext>
        <local:ViewModel/>
    </Window.DataContext>-->

    <ScrollViewer>
        <StackPanel Orientation="Vertical">
            <Grid x:Name="grid" local:GridHelpers.RowCount="100">
            </Grid>
        </StackPanel>
    </ScrollViewer>

    <!--<DataGrid ItemsSource="{Binding Speciess}" FrozenColumnCount="1" AutoGenerateColumns="True" IsReadOnly="True">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Number" Binding="{Binding Key.Number}"/>
            <DataGridTextColumn Header="Species 1" Binding="{Binding Value[0].Id}"/>
        </DataGrid.Columns>
    </DataGrid>-->
</Window>
